services:
  api:
    build: .
    container_name: trace_api
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - POSTGRES_HOST=db
    # depends_on:
    #   - db
    volumes:
      - .:/app
      # Optional: mount the private key if needed
      # - ./github_app_private_key.pem:/app/github_app_private_key.pem

  # db:
  #   image: postgres:15-alpine
  #   container_name: postgres
  #   restart: always
  #   env_file:
  #     - .env
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

  # adminer:
  #   image: adminer
  #   container_name: adminer
  #   restart: always
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     - db
  
  # pgadmin:
  #   image: dpage/pgadmin4
  #   container_name: pgadmin
  #   restart: always
  #   environment:
  #     PGADMIN_DEFAULT_EMAIL: admin@example.com
  #     PGADMIN_DEFAULT_PASSWORD: supersecretpassword
  #   ports:
  #     - "5050:80" # Access at http://localhost:5050
  #   depends_on:
  #     - db

volumes:
  postgres_data: